/**
 * Copy to your static site as js/dedalo-config.js (do not commit secrets).
 */
window.DEDALO_CONFIG = {
  pocketbaseUrl: "https://pb.dedalo101.com",
  artistSlug: "your-artist-slug",
  /** Optional: override default retry count for DedaloPB */
  retries: 3,
};