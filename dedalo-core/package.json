{
  "name": "dedalo-core",
  "version": "1.0.0",
  "private": true,
  "description": "Dedalo101 zero-cost artist management — PocketBase backend, static sites, artist dashboard",
  "scripts": {
    "build:static": "node scripts/build-static.js",
    "dev:dashboard": "npx --yes serve dashboard -p 8080",
    "format": "prettier --write \"**/*.{js,json,html,css,md}\""
  },
  "dependencies": {
    "pocketbase": "^0.27.0"
  },
  "devDependencies": {
    "prettier": "^3.5.3",
    "serve": "^14.2.4",
    "tailwindcss": "^4.3.0"
  },
  "engines": {
    "node": ">=20"
  }
}